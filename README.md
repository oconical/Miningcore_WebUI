# Miningcore_WebUI
Web UI that runs on standard html servers for Miningcore using the port API calls. Tested with RetroMike's verson of Miningcore. Can be installed directly into a www folder or can be installed along with the Apache Docker install script. If deploying publically, change the favicons and support link urls.
